<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <title>Inventario</title>
  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      font-size: 12px;
      color: #333;
      margin: 40px;
    }

    .header {
      display: flex;
      align-items: center;
      border-bottom: 2px solid #2196f3;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }

    .logo {
      width: 80px;
      height: auto;
      margin-right: 15px;
    }

    .title-group {
      flex-grow: 1;
    }

    .title {
      font-size: 20px;
      font-weight: bold;
      margin: 0;
      color: #1565c0;
    }

    .subtitle {
      font-size: 13px;
      color: #555;
      margin: 4px 0 0 0;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th,
    td {
      border: 1px solid #ccc;
      padding: 8px;
      text-align: left;
    }

    th {
      background-color: #e3f2fd;
      color: #1565c0;
      font-weight: bold;
    }

    tbody tr:nth-child(odd) {
      background-color: #f9f9f9;
    }

    .footer {
      margin-top: 40px;
      font-size: 11px;
      color: #777;
      text-align: center;
    }
  </style>
</head>

<body>

  <div class="header">
    <img src="<%= logoUrl %>" alt="Logo Farmacia" class="logo" />
    <div class="title-group">
      <p class="title">Inventario</p>
      <p class="subtitle">
        Fecha: <strong>
          <%= new Date().toLocaleDateString('es-PE', { year: 'numeric' , month: 'long' , day: 'numeric' }) %>
        </strong>
      </p>
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th>Producto</th>
        <th>Stock</th>

        <th>Unidad de Medida</th>
        <th>Precio Compra</th>
        <th>Precio Venta</th>
        <th>Fecha de Vencimiento</th>
      </tr>
    </thead>
    <tbody>
      <% inventario.forEach(item=> { %>
        <tr>
          <td>
            <%= item.nombre %>
          </td>
          <td>
            <%= item.stock %>
          </td>

          <td>
            <%= item.unidad_medida %>
          </td>
          <td>S/ <%= parseFloat(item.precio_compra).toFixed(2) %>
          </td>
          <td>S/ <%= parseFloat(item.precio_venta).toFixed(2) %>
          </td>
          <td>
            <%= item.fecha_vencimiento ? new Date(item.fecha_vencimiento).toLocaleDateString('es-PE', { year: 'numeric'
              , month: 'long' , day: 'numeric' }) : 'N/A' %>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table>

  <div class="footer">
    Farmacia - Sistema de Gestión | Reporte generado automáticamente
  </div>

</body>

</html>