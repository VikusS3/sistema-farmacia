<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Reporte de Ventas por Año</title>
  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      font-size: 12px;
      color: #333;
      margin: 40px;
    }

    .header {
      display: flex;
      align-items: center;
      border-bottom: 2px solid #2196f3;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }

    .logo {
      width: 80px;
      height: auto;
      margin-right: 15px;
    }

    .title-group {
      flex-grow: 1;
    }

    .title {
      font-size: 20px;
      font-weight: bold;
      margin: 0;
      color: #1565c0;
    }

    .subtitle {
      font-size: 13px;
      color: #555;
      margin: 4px 0 0 0;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 8px;
      text-align: left;
    }

    th {
      background-color: #e3f2fd;
      color: #1565c0;
      font-weight: bold;
    }

    tbody tr:nth-child(odd) {
      background-color: #f9f9f9;
    }

    .footer {
      margin-top: 40px;
      font-size: 11px;
      color: #777;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="header">
    <img src="<%= logoUrl %>" alt="Logo Farmacia" class="logo" />
    <div class="title-group">
      <p class="title">Reporte de Ventas por Año</p>
      <p class="subtitle">Desde: <strong><%= anio %></strong></p>
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Cliente</th>
        <th>Usuario</th>
        <th>Fecha</th>
        <th>Total</th>
        <th>Descuento</th>
        <th>Adicional</th>
        <th>Método Pago</th>
      </tr>
    </thead>
    <tbody>
      <% ventas.forEach(venta => { %>
        <tr>
          <td><%= venta.id %></td>
          <td><%= venta.cliente_nombre %></td>
          <td><%= venta.usuario_nombre %></td>
          <td><%= venta.fecha.toLocaleDateString('es-PE', { year: 'numeric', month: 'long', day: 'numeric' }) %></td>
          <td>S/ <%= parseFloat(venta.total).toFixed(2) %></td>
          <td>S/ <%= parseFloat(venta.descuento).toFixed(2) %></td>
          <td>S/ <%= parseFloat(venta.adicional).toFixed(2) %></td>
          <td><%= venta.metodo_pago %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="footer">
    Farmacia - Sistema de Gestión | Reporte generado automáticamente
  </div>

</body>
</html>
